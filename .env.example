# Optional: Ollama endpoint (defaults to http://localhost:11434/api/generate)
# OLLAMA_URL=http://localhost:11434/api/generate

# Optional: Ollama model to use for coaching feedback (defaults to gemma3)
# OLLAMA_MODEL=gemma3
